# Тестовое задание на вакансию PHP разработчика

Напишите Laravel проект, в состав которого обязательно входит:

1. Консольная команда, которая каждые 5 минут получает информацию от любого API на ваш выбор и сохраняет её в таблицу БД.

2. Route, отдающий массив записей таблицы в формате JSON.

Пример API:

https://official-joke-api.appspot.com/random_joke

```json
{
  "type": "general",
  "setup": "When a dad drives past a graveyard: Did you know that's a popular cemetery?",
  "punchline": "Yep, people are just dying to get in there",
  "id": 50
}
```

---

Необходимо написать JS-код, который в зависимости от выбранного значения поля "Тип" отражает разный набор полей на странице:

http://test.amopoint-dev.ru/testzz/testlist.html

Должны отображаться только те поля, в атрибуте `name` которых есть значение выбранного элемента списка.

Решение должно представлять из себя:
- файл для подключения к странице,
  или
- сниппет для запуска в браузере в консоли.

Допускается использование сторонних библиотек при условии обоснования их использования.

При разборе выполненных заданий при прочих равных важнее алгоритм решения.

Будет плюсом перечисление алгоритмов решений-аналогов и почему не были выбраны эти варианты.

---

# Дополнительное задание

Написать счетчик посещений страницы.

Решение должно состоять из двух компонентов:

## Frontend

JS-код, который подключается к любому сайту.

Скрипт должен:
- собирать необходимые данные:
    - ip,
    - город,
    - устройство;
- отправлять данные на сервер.

## Backend

Backend-часть должна:
- хранить данные в БД (SQLite или другая БД на выбор),
- показывать:
    - график посещений по часам (по оси х - количество уникальных посещений за час, по оси y- время),
    - круговую диаграмму с разбиением по городам.

Требования:
- оформить в виде страницы просмотра статистики;
- добавить авторизацию;
- выложить решение на любой хостинг для проверки.

---

После выполнения задания необходимо направить ссылку на репозиторий с решением в чат на hh.ru.